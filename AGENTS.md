# AGENTS.md

本文件为 Claude Code / Codex CLI / 其他自动化 agent 在**本仓**工作时提供指导。

> **这是什么**见 [`README.md`](README.md)；**设计契约**见
> [`docs/INGEST_KERNEL.md`](docs/INGEST_KERNEL.md)；**字段对账**见
> [`docs/rawevent-reconciliation.md`](docs/rawevent-reconciliation.md)；
> **扫描/投影的状态模型**（`AppendLogObservation` · `ScanReasons` × `CommitPlan` ·
> `Deadline` · `Probed` · 幂等 token）见
> [`docs/scan-state-model.md`](docs/scan-state-model.md)。
> 本文只写「动手前必须知道、否则会出事」的那些 —— 而本仓那些事**几乎全都来自
> 一个事实：它有多个互不知情的消费者**。

---

## 🔴 回复语言：中文

**面向人的输出一律用中文** —— 对话回复、总结、汇报、跨会话消息，全部。

**保持原样的**：代码本身、标识符、技术术语（`non_exhaustive` / `match` / commit 的
`<type>(<scope>)` 前缀 / trailer）。本仓代码注释与文档本来就是中文，照旧。

> ⚠️ **判例（2026-09-02，本轮实测）**：同一轮里**两种输出用了两种语言** ——
> 跨会话发给另外两个仓的消息全是中文，而每一次**给人的总结**都是英文。
> 不是忘了这条规则，是**只在其中一类输出上执行了它**。
>
> 🔴 判据因此不是「记得用中文」，而是**「面向人的输出」是一整类，不分场合**：
> 给同侪的、给用户的、口头总结、写进文档的，都在里面。

---

## 🔴 这是一个**公开仓**

写进来的每一个字都会被外人读到。**不得出现**真实用户名、内部主机名、个人路径
（`/home/<真名>/…`、`C:\Users\<真名>\…`）、真实项目名、邮箱。一律写成
`<user>` / `<distro>` / `<repo>` / 「宿主 A」。

⚠️ **数字要保留** —— 「96.8 万条事件一条没被看过」这种数字**就是这些规则的价值本身**。
脱敏脱掉的是身份，不是证据。

🔴 **但「本仓的证据」与「别人仓的内部状态」是两回事**（2026-09-02 判例）：跨会话协作时
会拿到消费方仓的内部数字（待清理项计数、内部工单号、未公开的缺陷清单）——
**那些不是本仓的证据，写进来就是替别人公开他们的内部状态**，尤其当那个仓是私有的。
⚠️ **脱敏闸抓不到这一类**：它查真实用户名 / 路径 / 邮箱，而这类内容里一个都没有。
⇒ 判据：**引用一个数字前问「这个数是我在本仓量出来的，还是别人告诉我的他们那边的？」**
后者要么略去，要么改写成不含来源的一般形式。

**写成什么，按位置分**（2026-08-24 定，见下方判例）：

| 位置                              | 写法                             | 为什么                                                       |
| --------------------------------- | -------------------------------- | ------------------------------------------------------------ |
| 散文、文档、`//` `///` `//!` 注释 | `<user>` / `<distro>` / `<repo>` | 尖括号一眼就是占位，读的人不会误当真值                       |
| **测试 fixture 的字符串字面量**   | 中性字面量（`dev` / `me` / `u`） | 把 `<user>` 放进一条当作真路径用的字符串，读起来像模板没渲染 |

🔴 **闸：`python scripts/check-public-safe.py`** —— 动完 `src/` 或 `docs/` 跑一遍，
**`git push` 之前再跑一遍**。它按**白名单**判（不在名单里的 `/home/<名字>` 一律报），
所以新加一个占位名是一个**要写下来的决定**，而不是一次没人会注意到的漂移。

⚠️ **它扫两样东西，别只想着文件**：整个工作区（`os.walk`，只跳过 `.git` / `target` /
`node_modules` / 编辑器目录）**和 `origin/main..HEAD` 的 commit message**。
后者 2026-09-02 才补上 —— 在那之前，`git push` 上传的内容里**有一半从来没被检查过**：
message 和文件一样进公开历史，而改它要 rebase 重写。
（换范围：`python scripts/check-public-safe.py <rev-range>`。）

📌 **两个数都要看**：它报「扫了 N 个文件」**和**「另扫了 M 条 commit message」——
**报条数不报「有没有」**，因为「0 条待推提交、没问题」与「一条都没检查」在布尔上
一模一样（见「第二条地基」）。M 是 0 时，那句「干净」没覆盖任何 message。

⚠️ **它自己先证明自己没坏**：`self_test()` 拿一份已知有问题的样本喂给同一套规则，
检不出来就退出非零、根本不去扫仓库 —— 因为**一个坏掉的扫描器和一个干净的仓库，
输出一模一样**。

📌 **正反两个方向都要自检**，别只做一半：`self_test()` 既断言「漏掉
`/home/<虚构名>` 就退出」（防**假阴** —— 规则写错了什么都抓不到），也断言
「把 `example.*` 当成真实邮箱就退出」（防**假阳** —— **一个满屏误报的扫描器
等于一个关掉的扫描器**）。假阳和假阴是两件事，各要一条对照。

### 🔴 它**不**覆盖的第三样：git 历史里的文件路径（已知缺口，故意不补）

工作区和 commit message 是两份清单，而**还有第三份**：删掉的文件，它的**路径名**
仍然留在每个提交的元数据里（`git log --stat` / `git log -p --all` 都搜得到）。
前两样的检查一个都覆盖不到它。

⇒ 真正的判据是「**别人克隆下来之后，`git log -p --all` 里还能搜到什么**」，
而不是「工作区干净吗」。

**为什么现在不补**（2026-09-02 决定）：本仓**一直**是公开仓，历史里没有过私有内容 ——
补它属于为一个**不存在的风险**加代码，而那份代码将来同样要维护、同样会漂。

🔴 **什么时候必须补**：① 某个私有内容曾经进过历史（哪怕后来删了文件）；
② 有仓从私有转公开 —— 那时**工作区**、**commit message**、**历史里的路径**是
**三份清单，不是一份**，逐份清。

⚠️ 写下这一节本身就是处置的一部分：**「没做」与「没想到」在仓库里长得一样**，
而下一个人只有看见「知道、且故意没做」才推翻得了这个决定（见「边界之外」第 3 条）。

> 🔴 **判例（本节存在的理由）**：2026-08-08 做过一次「安全审计」并**判它干净**。
> 那次查的是密钥 / 邮箱 / 账号 id，**没查用户名与路径** —— 而 15 个文件里的
> `/home/<真名>/…` 和几个私有项目名一直留着，直到 2026-08-24 才被翻出来（59 处）。
> **审计的范围和它的结论一样重要**，而「干净」这个词把范围藏了起来。
>
> ⚠️ 同一天那个扫描器的第一版报了 47 处，**一条真问题都没有**（正则过宽，把
> `/home/me`、`/home/...`、`-home-<user>-workspace-Proj` 全报了）。
> **一个满屏误报的扫描器等于一个关掉的扫描器** —— 噪声淹掉真信号，然后没人再看它。

---

## 🔴 本仓有**两个互不知情的消费者**，而它们拿到的版本由**两件不同的事**决定

实测核过（2026-08-21）：

| 消费者       | 怎么用本仓                                                    | 它拿到哪个版本，由什么决定 |
| ------------ | ------------------------------------------------------------- | -------------------------- |
| **QuotaBar** | 当 **Rust 库编译进去**（`features = ["store"]`，path 依赖）   | **它自己的子模块指针**     |
| **TumeFlow** | 子模块 + 把 **`svault` 可执行文件内嵌进 PyInstaller onefile** | **它上一次重新冻结的时刻** |
| **TumeChat** | ❌ **不碰**（无子模块、代码零引用、总库路径没写死）           | —                          |

**两个消费者，两条完全独立的版本传播路径。** 这一行是本文其余部分的地基 ——
下面每一条规则都是它的推论。

> 🔴 **这张表回答的是「版本怎么传播」，不回答「谁会来改本仓」。** 两个问题的答案
> 不同，而把前者当后者用过一次（2026-08-27）：`TumeChat` 那一行的 ❌ 被读成
> 「它与本仓无关」，据此差点判定不必给它留任何指针。
>
> **实际上 TumeChat 是本仓的利益相关方，只是还不是代码消费者。** 它的产品第一条
> 独特优势直接建立在总库上（「别人有一个空白的对话框，你有**全部**会话历史」），
> 而它自己的文档写着那个闭环今天是断的 —— 缺的那一步**要在本仓做**，见下方
> 「新增一个消费者」的生产侧。
>
> 判据：**「谁持有一份 svault」与「谁的需求会变成本仓的工单」是两张表。**
> 前者决定版本错配风险（本节），后者决定该给谁留指针。**一个 ❌ 只答了第一张。**

---

## 🔴 第二条地基：**一个缺失的动作，和一个正常的结果，在输出上分不出来**

上一节是「多个互不知情的消费者」。**这一节是另一条，而本仓一大半代码是它的产物：**

> **没做成某件事**，和**做成了、答案恰好是空的** —— 两者产出同一个输出。

⇒ 于是「什么都没发生」既可能是真的，也可能是一次静默失败，**而没有任何东西会说话**。

### 它长什么样（左列都是「缺失的动作」，右列是「正常的结果」）

| 左：没做成 | 右：真的是这样 | 本仓的分法 |
| --- | --- | --- |
| 探测失败 / 权限不够 | 这个目录**不是**仓库 | `Probed::Unknown` ≠ `Absent` |
| 那个目录**已经不在了** | **确认**这个仓没有 remote | `CheckoutMissing` ≠ `NoIdentity` |
| **没拿到**挂载表 | 拿到了，机器上就是没有映射 | 报**条数**，不报「有没有」 |
| **没算**这一项 | 算了，结果是 0 | `Option<usize>`：`null` ≠ `0` |
| 整轮**没扫成** | 扫了，零结果 | `IdentitySweep.unreadable` 单列 |
| 本轮**不写库** | 库里确实没有前代 | `PriorProjection::NoStore` ≠ `Absent` |
| 「**工具用不上**」 | —— 实为「我没想到怎么用」 | 见「边界之外」（2026-09-02） |
| 信号**没发出**（值在库内，没进线上） | 本仓不区分这个 | `Out::Source` 漏发 `artifact_kind` |
| 信号发了、**被收的人丢了** | 这一步没有输出 | `2>/dev/null` 吞掉 `unsupported glob` |

🔴 **最后两行是一对镜像，2026-09-02 一天之内各栽一次**：一个是「发了没人收」，
一个是「有没发出」。⚠️ **共同点是两端各自都觉得自己没问题** —— 发的人看库内的值是对的，
收的人看自己的解析是对的，而线上少一格。⇒ **跨进程的这一族，单看任何一侧都查不出来。**

⏳ **它还有一个时间版本**（消费方 2026-09-02 提，本仓的 `CheckoutMissing` 正是它的实例）：

> 一条**已经过期**的信息，和一条**仍然成立**的信息，在读取时分不出来。

⚠️ 而且它比空间版更难治：空间版缺的是「一个能说出差别的字段」，**时间版缺的是触发** ——
没有任何事件会在它失效时发生（目录是在别处被删的、待办是在别处做完的）。
⇒ `CheckoutMissing` 之所以修得掉，是因为**下一次扫描会撞见它**；撞不见的那些，
读取侧结构上就够不到，只能在**写入时**标记。

📌 **一个能直接用的问句**：**「被我拿来分类的那个量，会不会自己变？」**
判例（2026-09-02）：同一个会话，消费方上午量到 **58,514** 事件、几小时后本仓端到端
跑它是 **58,786** —— 「超上界」**不是一个静态属性，是一个当下的读数**。
⚠️ 而按它分出来的类若配了「跳过」这种处置，那个处置会**自己扩大作用域**：
今天不超的明天会超，且没有任何东西会说这件事。

**规模**：`src/` 里「没问成」出现 **143 次**，`Probed` / `GitRoot` / `IdentityVerdict` /
`PathIdentity` / `RootReach` 五个枚举整个是为这件事存在的。
**代价的判例**：把左右压成一格，藏了**一个多月**，代价是 **96.8 万条事件一条没被
看过**，而界面全程正常（见判据二）。

### 🔴 判据

1. **每写一个「空 / false / 0 / None」，问一句：它是右列，还是左列？**
   分不出就是缺陷 —— 哪怕**当下**两者的处置恰好相同（那种「结论相同、含义不同」
   最容易漂，因为它不会让任何东西变红）。
2. **给左列一个自己的名字。** 不是加注释，是加变体 / 加字段 / 报计数。
3. ⚠️ **压掉是允许的，但要写明「我知道这里压过一个三态」** —— 判例：
   `scan.rs::snapshot_mtime` 两支都归 `None`，却拒绝写 `_ =>`，就是为了让下一个
   拿这个值去做别的判断的人第一眼看见。
---

## 🔴 判据一：改本仓之前，问「**另一个消费者编不编得过**」

本仓的测试**只能证明本仓自洽**。它们不知道 QuotaBar 怎么用这个库 —— 不知道它从
结构体字面量构造 `Cursor`、不知道它直调 `scan_append_log_observed` 而绕开
`scan()`、不知道它依赖哪些 `pub` 项。

> **跨消费者的判据只有一个：在那个消费者的仓里编一次。**
> 本仓全绿 **≠** 消费者编得过。

推论，动手时按这个顺序：

1. **优先加法式改动** —— 只加不删、不改签名。`pub` 项一旦被消费者用上，删它/改它
   就是一次跨仓破坏，而本仓的 CI 一个字都不会说。
2. **给公开类型加字段前先想清楚**。消费者可能用**结构体字面量**构造它 ——
   那时加一个字段就直接让宿主编不过。判例：`Cursor` 被 QuotaBar 以字面量构造，
   所以 `scan-all --write-store` 需要的新状态存进了 **CLI 自己的**状态结构，
   而不是塞进 `Cursor`。
   - 🔴 **但「编不过」只是响的那一半。** 字面量构造会当场红，而**读取式用法会安静地
     少一格** —— 消费者若把字段逐个喂进 `format!`，加一个字段**照样编过**，只是从此
     少报一项。判例（2026-09-02，`IdentitySweep` 加 `checkout_missing`）：消费方
     一处引用，恰好是日志读取。⇒ **给报告型结构体加字段时，别只想「谁构造它」，
     还要想「谁在逐字段读它」** —— 后者没有任何编译器守得住。
3. **`#[cfg(feature = …)]` 的收紧是减法**。给一个原本无门的 `pub mod` 加门，等于
   对没开那个 feature 的消费者删掉了它。
4. **改完在消费者仓里编一次；编不了就明说没验** —— 别让「本仓全绿」被读成
   「跨仓没问题」。

⚠️ **不要顺手去动消费者仓的子模块指针。** 指针不动 = 那个仓一个字节都不受影响，
它编的还是老版本。bump 是**那个仓的会话**的事，那边有自己的全套验证。

---

## 🔴 判据一之推论：**注释拦不住调用方** —— 判据能进类型，就别留在注释里

本仓有多个消费者，而它们**不读你的注释**。一个只写在 doc comment 里的约束，
在别人的仓里就是一句「记得要这样」——而「记得」在跨仓的距离上不成立。

### 判例（2026-08-21，实测）

`CommitPlan::plan` 的第三个参数曾是 `has_prior: bool`。判据（**这个答案只能来自
总库，不能拿调用方自己的游标/缓存当代理**）当时写在 `TotalStore::has_projection`
的注释里，措辞相当明确。

结果：QuotaBar 传进来的正是 `cached.is_some()` —— **它自己 DB 里的一行**，
恰恰是那段注释点名禁止的东西。而两个方向都会错：

| 情形                                           | 拿游标当代理会判成 | 后果                                                                     |
| ---------------------------------------------- | ------------------ | ------------------------------------------------------------------------ |
| 手上没游标、库里有前代（另一个消费者早写满了） | 「没有」           | 真实的回退被记成追加，两层从此不一致                                     |
| 手上有游标、库里没有（库被删过/重建过）        | 「有」             | 开一个**没有前代可取代的空代**，而 `Rollback` 那代按设计**永不自动回收** |

**`bool` 不携带出处**，所以这两种误用在类型上都合法，编译器一个字都不会说。

### 改法与判据

判据搬进类型：`PriorProjection` 字段私有，构造入口只有两个 —— 要么
`ask(&store, &key)`（**问库**），要么 `no_store()`（**说自己没库可问**）。
「拿游标当代理」这句话现在**写不出来**。

🔴 **三态，不是两态。** 一个消费者可以整轮不写库，那时既不是「有前代」也不是
「没前代」。`NoStore` 与 `Absent` 在投影那一维上处置**恰好相同**（都落 `Append`
—— 不确定时不做不可逆的那一步），但含义不同 —— 而**「结论相同、含义不同」正是
最容易漂的那种**，因为它不会让任何东西变红。

### ⚠️ 三条边界，别过度套用

1. **不是所有判据都进得了类型。** 能进的是「这个值的**出处**」这类约束 ——
   出处正是原始类型丢掉的东西。「这个数应该小于那个数」之类进不去，别硬塞。
2. **不许给测试留后门。** `present_for_test()` 之类等于把刚立的闸拆在**最容易被
   复制**的地方 —— 下一个人会照着测试写生产代码。本例的 13 条既有用例改成了
   真的开一个内存库再 `ask()`；那点麻烦就是这道闸的价格。
3. **`compile_fail` doctest 会假绿。** 它只要求「编不过」，**任何**编译错误都算
   通过，一个拼写错误就能让它绿。所以必须配一条**必须编得过**的对照，并做变异
   （本例：把字段改成 `pub` ⇒ 那条 doctest 当场变红；还原后绿且**字节一致**）。

📌 这是本仓第三次用「让编译器守住」代替「写一条规则」：前两次是
`clippy.toml`（禁整个 `std::fs` 模块面，而不是用正则匹配拼写）与
`CommitPlan` 自己（字段私有 ⇒ 非法组合构造不出来）。

### 🔴 边界之外：**降级之前，先穷尽「工具真的用不上吗」**（2026-09-02）

上面三条讲的是「判据进得了类型」。**进不去的时候呢？** 先别急着回答进不去。

> **降级原则**：判据真的进不了类型时，退而求其次是「让下一个人能*机械地*核对」，
> 而不是「让下一个人*细心*」。

⚠️ **但这一节的原判例，当天就被推翻了 —— 留着，因为推翻它的过程比原判例值钱。**

原文写的是：「`IdentitySweep` 逐字段喂进 `format!` 的用法，**编译器守不住**」，
据此论证要降级到「可数的两列」。**几小时后消费方证明编译器守得住** —— 换个写法就行：

```rust
// 逐字段「读」 ⇒ 上游加字段，安静少报一格
log::info!("… {} {} {}", sweep.registered, sweep.recorded, sweep.no_remote);

// 逐字段「解构」 ⇒ 上游加字段，当场 error[E0027]: pattern does not mention field
let IdentitySweep { registered, recorded, no_remote, unreadable: _, .. } = &sweep;
//                                                                   ↑ 🔴 写 `..` 等于原样撤销这条保证
```

🔴 **所以那不是「工具用不上」，是「我没想到该怎么用」** —— 而这两者在当时长得
一模一样：**都表现为「我找不到工具」**。

⇒ **判据顺序**（这一节真正要立的）：

1. **先穷尽**：换个写法能不能让编译器接手？（读 → 解构、`bool` → 类型、注释 → 私有字段）
2. **确实不能**，才降级到「机械可核对」，且降级后仍要有判据（「数得出来」）。
3. **降级要留痕**：写清「为什么工具用不上」—— 否则下一个人无从推翻它，
   而上面这次推翻**正是因为处置写在了明处**。

⚠️ **过早降级和正当降级，事后看长得一模一样** —— 两者都是「有一个能核对的办法」。
区别只在第 1 步做没做过，而**那一步不做也不会有任何东西报错**。

📌 **变异要打在护栏声称要防的那条路径上**（本仓老规矩，见上方第 3 条与 `scripts/mutate.py`）：
这条护栏的变异对象是**上游加字段**（`cargo check` 当场 `E0027`），不是「本地删一行」——
后者只证明「不能少写」。

---

## 🔴 判据二：运行时**有两份 svault 同时开着同一个总库**，而子模块指针拦不住这件事

```text
QuotaBar   →  编译进去的那份（版本 = 它的子模块指针）
TumeFlow   →  内嵌 onefile 的那份（版本 = 它上次冻结的时刻）
                        ↓  同时打开
        <data_local_dir>/svault/total_store.db
```

**谁先启动谁跑 `migrate()`。** 而子模块指针是**编译期**的闸，schema 迁移是**运行时**的
—— 它拦不住。两个消费者可以差好几个版本，然后一起打开同一个文件。

### 这已经咬过一次，而且**咬了一个多月没人发现**

装机的那份 `svault` 落后一个月、缺 5 个子命令。两个调用点 `catch` 之后**静默降级**
⇒ **96.8 万条事件一条没被看过**，界面全程正常，**持续一个多月**。

🔴 **它之所以能藏这么久，是因为「问不到」和「本来就没有」长得一模一样。**
所以本仓的对外报告面到处是「三态」而不是布尔（`Probed<T> { Found, Absent, Unknown }`）
—— 那不是洁癖，是这次事故的直接产物。

**判据是「跑它、问它会什么」，不是「文件在不在」。** 消费者侧应做**正向断言**：
拿到版本号、拿到子命令列表，而不是「没报错就当它在」。

### 改 schema 之前必须明确问一句：**落后一个版本的消费者读不读得动**

⚠️ **不要假设「迁移都是加法式的所以旧版还能读」** —— 那句话在本仓**不成立**。
建表确实是 `CREATE TABLE IF NOT EXISTS`、加列确实是 `ADD COLUMN`，**但核心表做过
整表重建**：

- `raw_events`：`ALTER TABLE … RENAME` → 建新表 → `INSERT INTO … SELECT` → `DROP TABLE`，
  过程中 **`generation` 列被 `source_revision` + `projection_revision` 取代**
  —— **一个列消失了**。任何还在 SELECT 它的旧版当场就炸。
- `project_identity`：同一形状的整表重建（`…_pre_rootkey` → drop）。

⇒ **「旧版还读得动」是纪律，不是构造。** 每次改 schema 都要重新问，不能援引惯例。

---

## 「新增一个消费者」之前先问什么

🔴 **真正的约束不是「谁有权读总库」，是「有几份独立版本的 svault」。**

每多一份**可能落后**的 svault，就多一条静默版本错配的路 —— 而上面那次事故说明，
这种路**不会报错**，只会让某个数字长期为零。

所以新消费者只有两个合法姿势：

|                                   | 做法                            | 代价                                                   |
| --------------------------------- | ------------------------------- | ------------------------------------------------------ |
| **复用已有的一份**                | 经已有消费者的 RPC / CLI 拿数据 | 多一跳，且那个消费者的 API 面会被撑大                  |
| **明确接受自己成为第 N 个所有者** | 自带一份 svault                 | **必须同时给出版本协调方案** —— 不给就是把事故重放一次 |

**没有第三种。** 「先接上再说，版本问题以后再管」就是那一个多月的复现步骤。

### 已经想过的一个具体场景：TumeChat 将来要读会话

（**尚无 ADR** —— 写在这里是为了免得下次从头再推一遍。）

先把它拆成两件事，因为**只有一半需要本仓**：

|                                                        | 需要 SessionVault 吗                  |
| ------------------------------------------------------ | ------------------------------------- |
| TumeChat 读**自己产**的会话                            | ❌ **不需要** —— 它就是那些会话的源头 |
| TumeChat 读**其它 agent**（Claude Code / Codex）的会话 | ✅ 需要                               |

三条路，各自的代价：

|       | 做法                                        | 代价                                                                                                 |
| ----- | ------------------------------------------- | ---------------------------------------------------------------------------------------------------- |
| **A** | TumeChat 自带 svault 直读总库               | 🔴 **第三个所有者**；而且它是 Node，得 spawn 一个 exe —— 与「client 不得内嵌引擎的私有副本」那条冲突 |
| **B** | TumeFlow 加一个**窄**的 `sessions.read` RPC | ✅ svault 仍然只有两份；⚠️ 但它把 TumeFlow 的 API 面撑成「原始证据代理」，而 TumeFlow 不是那个东西   |
| **C** | 会话先进记忆，TumeChat 只读记忆             | ✅ 最贴分层；❌ **丢保真度** —— 实测可解开的指针只有 **6/53**                                        |

⚠️ **今天这条路本来就不通**：TumeFlow daemon 的 **23 个** RPC 能力里**没有任何
`sessions.*`**（`sessions-recent` / `sessions-read` 只是它内部调 svault 用的，
从没往 RPC 上露过）。所以 B 不是「接一下」，是**新增对外 API**。

🔴 **顺序上，消费侧依赖生产侧。** 让 TumeChat 的会话**进**总库（新增一个
`SourceType`）还在等它的会话格式落地。**那件事先做** —— 那时「统一的会话视图」才
成立，消费侧的需求才谈得清。**现在设计消费侧，是按一份还没定的契约写代码。**

### 那件事到底是什么：生产侧（2026-08-27 补）

上面那句「那件事先做」**没写那件事是什么**，于是这一节整节都在谈**读**，而眼下真正
排在前面的是**写**。工作量不是「加一个枚举值」，也不是「把会话落成兼容 JSONL」：

```
SourceType 变体
  + 来源发现描述符（去哪找、怎么认）
  + 解析器（TumeChat 会话 → RawEvent）
  + 稳定 source_key（重命名/移动后仍是同一条）
  + 删除传播映射（TumeChat 删会话 → 记忆侧怎么跟）
  + 增量与损坏恢复测试（写到一半、被截断、乱序）
```

⚠️ **「有枚举值」不等于「能用」**：今天 `SourceType` 已有 5 个变体，而
`Cursor` / `Gemini` / `Jsonl` **只校验 JSON、不产生 `RawEvent`** —— 照着「已经有 5 个了」
估工作量会差一整个解析器。

🔴 **这一步不触发上面那条「第三个所有者」的警告。** 加解析器**不需要 TumeChat 持有
任何一份 svault**：它只是把自己的会话写到磁盘上，由已有的扫描者摄取。
**「谁的格式进总库」与「谁持有 svault」是两件事** —— 混起来会把一件安全的事
按危险的那条否掉。

（工作量清单来自 TumeChat `docs/CROSS_REPO_CONTRACTS.md` §2.3；那边是需求方，
判据在这边，因为代码在这边。）

## 🔴 宿主答不了发行版内部的事（改 `decode_project_dir` / 任何 UNC 探测前必读）

> 本节与下一节 2026-08-24 从 QuotaBar 的 `AGENTS.md` 搬来（task #58）。
> **判据归代码所在的仓** —— 它们讲的是本仓 `probe` / `identity` / `pathnorm` 里的
> 代码，而直接 clone 本仓的人读不到那份文件。调用方那一侧的义务留在 QuotaBar。
>
> ⚠️ 搬运时按本仓「这是一个公开仓」那节脱过敏（真实用户名 → `<user>`，
> 私有项目名 → 中性名）。**原文里就带着它们** —— 不脱敏直接搬，等于把刚清掉的
> 个人信息又搬回公开仓。

- 🔴 **`\\wsl.localhost\…` 上宿主给的答案，只有三种是事实**：`Dir` / `File` /
  `Absent`（前提是父目录可遍历）。`Found(Other)` 与 `Unknown` **都不是答案** ——
  它们几乎总是一个**宿主跟不进去的符号链接**：踩在它身上是 `Other`，**穿过**它是
  `ERROR_DIRECTORY`（raw 267）⇒ `Unknown`。**同一个链接两种症状，只兜一种等于没修。**
  实测 `/home/<user>/workspace/QuotaBar -> /mnt/c/Users/user/workspace/QuotaBar`：
  解码报「这个项目不存在」⇒ 别名组 `len() >= 2` 让**整组消失** ⇒ 同一个目录的项目
  记忆分裂成互不可见的 37 + 24。用 `probe::WslUncBackend`。
- ⚠️ **不要因此得出「UNC 对 WSL 不可用」** —— 同机其它 WSL 项目走 UNC 一直正常。
  我最初把它解释成「进程之间行为不同」（Git Bash 的 `ls` 能列而本进程不能），
  **那是错的**，而照那个过宽的结论去设计会白付「每次探测起一个 `wsl.exe`」的代价
  （一次往返实测 **≈1.5 秒**，解码一条四段路径要问 10 个候选 = 7 个项目 30 秒卡顿）。
  判据是**「这条路径归谁管」**，不是「UNC 通不通」。
- **访问桥问什么，决定调用方能得到什么。** `wsl::stat` 问的是 `[ -f ]` ⇒ **每一个
  目录都会被它报成「不存在」**。要问目录用 `stat_kind`。这条 `WslBackend` 的文档
  从第一天就写着，只是到 2026-08-14 才有第一个调用方触发它。
- 🔴 **归属层换了后端，不等于发现层换了**（2026-08-14 装机后实测，本节当天就复发）。
  `discover.rs::probe_host_file` 用的是 `LocalBackend::rooted_at(anchor)`，而
  `discover_project_snapshots` 的 `(_, Some(base))` 分支对 **Local 与 Wsl 一视同仁**
  —— WSL 根带的 `probe_path` 是 UNC 形式，于是走了纯宿主后端。实测：

  | 问谁        | `/home/<user>/workspace/QuotaBar/CLAUDE.md` | `/mnt/c/…/QuotaBar/CLAUDE.md` |
  | ----------- | ------------------------------------------- | ----------------------------- |
  | 宿主（UNC） | 读不到                                      | 读不到                        |
  | 发行版自己  | **有**                                      | **有**                        |

  后果：WSL 侧那份 21 KB 的 `CLAUDE.md` **永远**进不了 Class-B。

  🔴 **我一度把「3 个根没有 `canonical_id`」也算进它的后果，那是错的**（当天订正）。
  `repo_id_for_root` 按 `split_canonical_wsl` 分派：`wsl:<distro>:/…` 走桥、**其余全走
  本机**。而那 3 个根是**裸 POSIX 形式**（`/home/<user>/workspace/QuotaBar`、
  `/mnt/c/…/QuotaBar`），在 Windows 上根本没有命名空间 —— 桥那条路本来就是通的
  （同一目录的 `wsl:Ubuntu-22.04:` 那行**有**身份）。**那是「注册进来的形式不对」，
  不是「问错了后端」，本条修不了它**（另立任务）。
  教训与本节同源：**症状挨着不等于同一个根因** —— 两件事都由「WSL 路径」触发，
  我据此把它们并成一条，而证据（`svault roots` 的 `canonical_id` 列）当时就在手上。

- 🔴 **修完立刻 grep 全仓问「这个判据还有第几处没换」。** 上一节记着同一句话，
  而第八次就是这么漏的：那次收口的是「怎么解读答案」，**没有收口「该问谁」**，
  于是 `ProbeBackend` 的选择仍散在每个调用点、选错时三态会忠实地报「没问成」。

---

## 🔴 身份是根的属性，不是观察者的（改 `project_identity` 前必读）

- 🔴 **主键不带 `source_type` / `source_location`** —— 与 `project_root_registry`
  同一条理由，而那条理由**早就写在隔壁那张表上**：「一个路径是不是项目根，与谁在
  什么位置扫到它无关；带上它们会让同一个根按发现者分裂成多行」。对身份一字不差地
  成立。实测就是那个形状：同一个根、同一个 `git:` 身份、**三行**，而唯一的消费者
  `project_roots_report` 把这两列**整个忽略**。
- 🔴 **身份由注册表驱动，不由事件驱动。** 挂在 `apply_projection` 上的那一版有个
  静默盲区：**近期没有活动的项目永远拿不到身份**，哪怕注册表认得它、`.git` 就在
  那儿。后果不是「少一行」—— TumeFlow 的 merge key 是「有身份用身份、没有退回
  路径」，**身份在与不在给出两个不同的 key**，同一份记忆随之落进不同的桶。
- **推论（比这个 bug 值钱）**：一条纪律如果**当前的数据结构表达不了**，先改结构，
  别在调用点凑。这里是「为注册表里每个根记一次身份」——注册表只有根、没有观察者，
  所以不先收敛主键就根本写不出那个函数。
- 🔴 **同一条理由再上一层：同一个目录的不同「写法」也不该分裂身份**
  （2026-08-15 实测）。注册表**故意**保留多种写法（`probe_wsl` 的注释：归属是纯
  字符串匹配，「登记什么形式就只认什么形式」；无条件转规范形会让一条
  `/home/<user>/workspace/VisionApp` 的 **106,814 条事件**全部归不到根）。归属层用
  **带挂载表**的比较键把它们收敛了，**身份层没有那一步** —— `project_identity` 按
  `project_root` 原样键，于是每种写法各自去解析。实测 20 个根里 **3 个**没有
  `canonical_id`（裸 POSIX 与 `/mnt/c/…`），而同一目录的规范形那行**有**。
  - 直接原因是「该问谁」有**两份实现**：`discovery::probe_path` 认四种形态，
    `identity::repo_id_for_root` 只认两种 ⇒ 裸 Linux 与 `/mnt/…` 落进本机分支，
    而在 Windows 上它们是**当前盘的相对路径**。现收口到 `pathnorm::reach_of`。
  - ⚠️ **`RootReach::Unknown` 不是「本机」**：落回本机要么探不到（被按「确认无根」
    缓存 24 小时），要么**误中**真实存在的 `\home\u\x`。
  - ⚠️ **别去改注册表的多写法**（会退回上面那个 106,814 条的缺陷），
    也**别给 `path:` 加兜底**（丢弃它是故意的）。

---

## 🔴 跨会话分工：谁改本仓、需求长什么样（2026-09-02 定）

> 本节是**正文**。两个消费者仓的 `AGENTS.md` 只写指针、不复述 —— **两份副本必然漂**，
> 而这正是本仓反复付过代价的那个形状。

### 前置：先问这一句

🔴 **「这件事能不能*不改本仓*就做完？」**

⚠️ 跨会话协调有**实测成本**：2026-09-02 为交接一份**文档**来回了 **8 轮**。
本仓每多一个改动，就多一次子模块指针传播 + 两个消费者各自重验。

**判例（2026-09-02，Class-A 读预算）**：那个需求**一半根本不需要本仓** ——
`read_sessions` 改成逐会话各调一次即可，零本仓改动（消费方当天实测过这个调用形态）。
只有「续读超上界的会话」那半才落到本仓。**先拆一刀，再提需求。**

### 五条规则

| #   | 规则                                                  | 判据                                                                                                |
| --- | ----------------------------------------------------- | --------------------------------------------------------------------------------------------------- |
| 1   | **代码改动在本仓的开发克隆里做**                      | 两个消费者仓的子模块 checkout 都有 pre-commit hook 会拒；且**只有本仓跑得了全套测试 + 公开仓脱敏闸** |
| 2   | **需求由消费方提，且必须带实测、不是结论**            | 见下                                                                                                |
| 3   | **各消费方自己 bump 自己的子模块指针**                | 只有消费方跑得了「我这边还用得了」那个验证                                                          |
| 4   | 🔴 **本仓合并后主动通知每个消费方**，不等它们自己发现 | 见「规则 4 补的是什么」                                                                             |
| 5   | 🔴 **通知里分开报每一类消费形状**（CLI / Rust 构造 / Rust 逐字段读取 / **discover 过滤**） | **同一个提交对三类的破坏性可以完全不同**，一句「有破坏性变更」指向三个动作；⚠️ 第三类**编译器守不住**、只会安静少报一格；见下判例 |

### 🔴 规则 2 的形状：传实测，不传结论

**判例（2026-09-02，真实）**：一个跨会话传来的、**标着 ✅ 的结论**是错的 ——
按它执行会删掉 4 份唯一副本。**结论把它依据的观测藏了起来，于是没人能复核它。**

```
❌ 传结论  「加个 skills 的 descriptor 就行」

✅ 传实测  「discover 现在返 683 个来源；~/.claude/skills 8 文件 +
            ~/.codex/skills 60 文件不在其中；要它们进 snapshot_file、kind=Skill；
            判据是 discover 之后这两个目录的文件数 > 0」
```

判据：**需求里要有一个「我这边怎么算它做完了」的可执行判据**，而不是一个结论。
⚠️ 数字保留（见「这是一个公开仓」那节）—— 脱敏脱掉的是身份，不是证据。

### 规则 4 补的是什么：一处**没有人做错**的规则缺口

实测（2026-09-02）：一个消费者的子模块指针跟到了 `27ecaff`，另一个还钉在
`865aa7b` —— **落后 16 个提交**，而其中新增的 `attribute` 子命令正是**落后那一方
自己挂着的一条待办**：它已经做完了，而那一方不知道。

🔴 **没有人违反规则 1–3。** 跟上的那一方按规则 3 bump 了指针，而**规则 3 从不
要求它通知别人** —— 缺的是规则 4。判据：**「每个人都照做了」与「事情没漏」是两
件事**，而只有前者能被检查出来。

### 🔴 规则 5 的判例：`865aa7b..27ecaff`，同一批提交，**三类消费方三个答案**

通知里要带**对消费方形状的判断**，而不只是一句「我合了」——
🔴 而**三类里只有第三类编译器守不住**（2026-09-02 消费方实测补）。

| 消费方 | 这批提交对它是 |
| --- | --- |
| **CLI**（跑二进制） | **全加性** —— 新子命令 `attribute`、新标志 `roots --duplicates`、`roots_summary` 多 4 个默认 `null` 的字段；`project_root` 行字段集**未变**；退出码语义**未变** |
| **Rust 库**（编译进去，**构造 / 穷举**） | **4 处破坏**（都无 `#[non_exhaustive]`）—— 新变体 `GitRoot::StartMissing` / `IdentityVerdict::CheckoutMissing` 让穷举 `match` 编不过；新 pub 字段 `RepoIdentity.checkout_missing` / `IdentitySweep.checkout_missing` 让结构体字面量编不过 |
| 🔴 **Rust 库（逐字段*读取*）** | **不编不过，而是安静少报一格** —— 把字段逐个喂进 `format!` / 日志的用法，加一个字段照样编过。实测：消费方对 `IdentitySweep` 唯一那处引用**恰好是这种**（一条 `log::info!`）。✅ **已有处置**：改成穷举**解构**（`let IdentitySweep { a, b, unreadable: _ } = &sweep`，**不写 `..`**）⇒ 上游加字段当场 `E0027`，这一格**退回第二格**。见「边界之外」 |
| 🔴 **`discover` 的过滤方** | **本批加性，但判据完全不同** —— 它按 `SourceMode` 过滤 `discover`（消费方实测：只取 `AppendLog` 当会话解析）。⇒ 该问的不是「加没加 pub 字段/变体」，是「**新描述符会不会落进它过滤的那个 mode**」。本批四类全是 `SnapshotFile`，`AppendLog` 描述符 **5 → 5、glob 逐字相同** |

🔴 **一句「有破坏性变更」对这四类指向四个不同的动作** —— 前两类「编不过」会自己
喊，后两类不会。**别只报一个，尤其别只报会喊的那两个。**

⚠️ **第四格是消费方自己发现的，不是我报出来的**（2026-09-02）：**我不知道它按
`AppendLog` 过滤，它不知道我新增的是什么 `mode`** —— 双方各自都看不见对方那半。
⇒ 报形状时**连新增描述符的 `source_mode` 一起报**，哪怕本批没有破坏：
`history.jsonl` 是 jsonl 且只增不改，**长得极像 `AppendLog`**，而它旁边那句
「这里用 `SnapshotFile` 是权宜」正是让对方一眼看清**这是有意降级、不是漏标**。

⚠️ **加性也可能咬人：值域变了。** `identity_verdict` 多了一个取值
`checkout_missing`（从前那些根报 `no_identity`）。字段没增没删，**但读它的分支会
静默换桶**。本机真库实测：24 个根里 1 个当场改判。
⇒ **报「形状」时要连值域一起报**，字段集不变不等于没变化。

📌 **没有消费者的字段，不会因为上游多一个取值而错判** —— 那个 CLI 消费方没被咬到，
正因为它的 `ProjectRoot` **故意不收**。⚠️ 但它的文档字符串里**抄了一份取值清单**，
当场就漏了新值：**抄上游枚举连过期都不需要，上游加一个值就错了。**

---

## 提交 / 分支

- commit 用 Conventional Commits（`<type>(<scope>): <subject>`，主题中文 ≤50 字）；
  author 用 GitHub noreply 邮箱，**勿用工作邮箱**。
- **禁止** `git add -A` / `git add .`，按文件名显式 stage。
- **禁止** `--no-verify` 跳 hook。
- 提交前跑 `cargo fmt --all` 与 `cargo test --lib --features store`。
  ⚠️ **feature 组合要逐个编** —— 门后的代码不在默认闸的覆盖里：
  `cargo check --bins`（默认）/ `--features store` 两条都要过。

---
